package repository;
/*
import model.Reserva;
import java.time.LocalDate;
import java.util.List;

/**
 * Contrato del repositorio de reservas. La implementación real con
 * XML la construye el Integrante 2 (ReservaXmlRepository).

public interface ReservaRepository {
    List<Reserva> buscarActivasPorFecha(LocalDate fecha);
    List<Reserva> buscarActivasEntre(LocalDate desde, LocalDate hasta);
}
*/